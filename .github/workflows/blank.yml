# This is a template workflow kept for reference only.
# It does not run automatically; use workflow_dispatch to trigger manually.

name: CI (template - disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run a one-line script
        run: echo Hello, world!
